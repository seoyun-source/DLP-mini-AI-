# 프로젝트 완료 보고서: DLP-mini
### AI 기반 개인정보(PII) 탐지 도구

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | DLP-mini: AI 기반 개인정보 탐지 도구 |
| 수행 기간 | 1주 |
| 수행 형태 | 개인 프로젝트 |
| 기술 스택 | Python, Streamlit, Gemini API(gemini-3.6-flash), 정규식(re) |
| 결과물 | GitHub 레포지토리 (코드 + 실행 가능한 웹 데모) |

---

## 2. 기획 배경 및 목적

개인정보보호법 강화와 DLP(Data Loss Prevention) 솔루션에 대한 기업 수요 증가라는
트렌드에 맞춰, 정규식과 생성형 AI를 결합한 하이브리드 개인정보 탐지 도구를 기획했다.
정규식만으로는 놓치는 문맥적 개인정보(이름+직책 조합 등)를 LLM으로 보완하는 것이
핵심 목표였다.

---

## 3. 시스템 구조

```
[사용자 입력 텍스트]
        │
        ▼
┌───────────────────┐
│  1차 필터: 정규식   │  → 전화번호, 이메일, 주민번호, 계좌번호 등
└───────────────────┘     (우선순위 기반 중복 탐지 방지 로직 포함)
        │
        ▼
┌───────────────────┐
│  2차 필터: Gemini   │  → 이름+직책 조합, 암묵적 위치 언급 등
│  API (LLM)         │     문맥적 개인정보 탐지
└───────────────────┘
        │
        ▼
┌───────────────────┐
│  결과 통합 및 마스킹 │
└───────────────────┘
        │
        ▼
[마스킹된 텍스트 + 리스크 리포트 출력]
```

---

## 4. 개발 및 트러블슈팅 과정

계획대로 흘러가지 않았던 지점들과 해결 과정을 시간 순으로 정리했다.
**이 과정 자체가 이 프로젝트에서 가장 실무적인 경험이었다.**

### 문제 1. 로컬 실행 환경 미비
- **현상**: `streamlit run app.py` 실행 시 `'streamlit' is not recognized` 오류
- **원인**: 필요한 패키지(streamlit, google-generativeai)가 설치되지 않은 상태였음
- **해결**: `pip install -r requirements.txt`로 의존성 설치. 이후 `requirements.txt`
  파일이 하위 폴더에 있어 경로 문제가 추가로 발생했고, `cd`로 올바른 디렉토리 이동 후 해결
- **배운 점**: 배포/실행 환경에서는 의존성 관리와 파일 경로 구조가 코드 로직만큼 중요하다

### 문제 2. API 모델 Deprecation
- **현상**: Gemini API 호출 시 `404 This model models/gemini-2.5-flash is no longer
  available to new users` 오류 발생
- **원인**: 개발 시점과 실행 시점 사이 Google이 모델 라인업을 변경함
- **해결**: 에러 메시지가 안내한 대체 모델(`gemini-3.6-flash`)로 코드 수정
- **배운 점**: 외부 API에 의존하는 서비스는 모델/버전 변경에 대한 대응 체계(예: 설정값
  분리, 버전 모니터링)가 필요하다는 것을 실감함

### 문제 3. API 할당량(Quota) 문제
- **현상**: API 호출 후 "탐지 중" 상태로 3분 이상 응답 없이 멈춤
- **원인**: 학교 계정(Google Workspace)으로 생성한 프로젝트가 조직 정책상 API 할당량
  등급이 제한되어 있었음 (`프로젝트 할당량 등급을 사용할 수 없습니다` 오류 확인)
- **해결**: 개인 Gmail 계정으로 새 프로젝트를 생성해 API 키 재발급, 정상 동작 확인
- **배운 점**: 조직 계정은 관리자 정책에 따라 예상치 못한 제약이 걸릴 수 있으므로,
  개인 프로젝트와 조직 계정을 분리해서 관리해야 한다는 것을 배움

### 문제 4. 정규식 패턴 간 중복 탐지 (오탐)
- **현상**: 전화번호(`010-1234-5678`)가 "전화번호"와 "계좌번호(추정)" 두 유형으로
  중복 탐지됨
- **원인**: 계좌번호 정규식 패턴(`\d{2,6}-\d{2,6}-\d{2,6}`)이 전화번호 형식과
  구조적으로 겹침
- **해결**: 정규식 패턴 딕셔너리의 정의 순서를 우선순위로 삼아, 이미 탐지된 구간(span)과
  겹치는 매치는 건너뛰도록 로직 개선
- **배운 점**: 패턴 매칭 기반 탐지 시스템은 개별 패턴의 정확도뿐 아니라 패턴 간
  상호작용(충돌)까지 설계 단계에서 고려해야 한다는 것을 직접 확인함

---

## 5. 최종 결과물

**입력 텍스트:**
```
김민수 대리님이 어제 강남역 근처 카페에서 미팅했다고 하셨어요.
연락처는 010-1234-5678이고, 이메일은 minsu.kim@example.com 입니다.
계좌번호는 123-456-789012 이며, 주민번호는 901231-1234567 입니다.
```

**마스킹 결과:**
```
[이름+직급 조합]이 어제 강남역 근처 카페에서 미팅했다고 하셨어요.
연락처는 [전화번호]이고, 이메일은 [이메일] 입니다.
계좌번호는 [계좌번호(추정)] 이며, 주민번호는 [주민등록번호] 입니다.
```

**리스크 리포트:**

| 탐지값 | 유형 | 탐지방식 | 판단근거 |
|---|---|---|---|
| 010-1234-5678 | 전화번호 | 정규식 | 정규식 패턴 일치 |
| minsu.kim@example.com | 이메일 | 정규식 | 정규식 패턴 일치 |
| 901231-1234567 | 주민등록번호 | 정규식 | 정규식 패턴 일치 |
| 123-456-789012 | 계좌번호(추정) | 정규식 | 정규식 패턴 일치 |
| 김민수 대리님 | 이름+직급 조합 | LLM(문맥) | 이름과 직급 조합으로 개인 특정 가능 |

---

## 6. 이 프로젝트를 통해 강조할 수 있는 역량

| 역량 | 근거 |
|---|---|
| 보안/개인정보보호 도메인 이해 | DLP 구조를 실제로 구현하며 개인정보보호법·비식별화 개념을 실무 관점에서 학습 |
| 기술 선택의 트레이드오프 판단 | 정규식(빠르고 저렴)과 LLM(문맥 이해)의 역할을 나눠 하이브리드 구조를 설계 |
| 프롬프트 엔지니어링 | JSON 구조화 출력을 강제하는 프롬프트 설계 및 파싱 실패 대비 방어 로직 구현 |
| 실전 트러블슈팅 능력 | 환경설정, API 버전 관리, 조직 계정 정책, 알고리즘 설계 오류까지 4가지 성격이
  다른 문제를 스스로 원인 분석하고 해결 |
| 실행력 및 완결성 | 기획→구현→배포(GitHub)→문서화까지 전 과정을 1주 내 완주 |

---

## 7. 한계 및 향후 개선 방향

- 오탐/미탐에 대한 정량적 검증(정밀도·재현율 측정) 미수행 → 추후 테스트 데이터셋
  구축 후 성능 지표화 필요
- 무료 API 티어의 속도 제한으로 대량 처리에는 부적합 → 실사용 시 유료 티어 또는
  경량 온프레미스 NER 모델 검토
- 텍스트만 지원 → 이미지(신분증, 명함) OCR 연동으로 확장 가능

---

## 8. 결론

이 프로젝트는 단순히 "AI API를 호출해봤다"는 수준을 넘어, **하이브리드 시스템 설계
판단, 외부 의존성 관리, 그리고 실행 중 발생한 문제를 스스로 진단하고 해결하는 과정**을
경험한 것에 의의가 있다. 특히 계획 단계에서 예상했던 한계(오탐 가능성)가 실제로
재현되고 이를 해결한 경험은, 이론적 이해와 실무 적용 사이의 간극을 직접 좁혀본
사례로서 면접에서 구체적으로 설명할 수 있는 자산이 되었다.